# LearnForge Backend - Environment Variables
# Copy this file to .env and fill in your values for LOCAL DEVELOPMENT

# ============================================================================
# LOCAL DEVELOPMENT ENVIRONMENT VARIABLES
# ============================================================================

# Agent Configuration
GOOGLE_GENAI_USE_VERTEXAI=FALSE
GOOGLE_API_KEY=your-google-api-key-here

# Server Configuration
HOST=0.0.0.0
PORT=8080

# CORS Configuration
# Comma-separated list of allowed origins
ALLOW_ORIGINS=http://localhost:8000,http://localhost:3000

# Firebase Configuration (Local Development)
# Path to your Firebase service account JSON file
GOOGLE_APPLICATION_CREDENTIALS=firebase_key.json

YOUTUBE_API_KEY=


# ============================================================================
# PRODUCTION DEPLOYMENT (Cloud Run via GitHub Actions)
# ============================================================================

# These are NOT loaded from .env
# Instead, configure them as described below:

# 1. GitHub Secrets (for CI/CD deployment):
#    - GCP_PROJECT_ID: Your Google Cloud project ID
#    - GCP_REGION: e.g., us-central1
#    - GCP_SA_KEY: Service account JSON for deployment
#    - CLOUD_RUN_SERVICE: Your Cloud Run service name
#    - ARTIFACT_REGISTRY_REPO: Your Artifact Registry repo name

# 2. Google Cloud Secret Manager (for runtime):
#    - firebase-service-account-key: Firebase credentials JSON
#    - google-api-key: Google API key
#    - allow-origins: CORS origins (comma-separated)

# See README.md "Setup GitHub Deployment" section for detailed instructions

